Para rodar o projeto é necessário, alterar a propriedade path.file para um caminho a seu critério.

Após fazer isso, rodar a aplicação e acessar a url: http://localhost:8080/calculate

Caso queira mudar os valores dos fatores, basta alterar as propriedades: factor.internal e factor.external do arquivo application.properties